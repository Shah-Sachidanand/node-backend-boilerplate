/**
 * A class that contains static properties for user-related messages.
 */
class UserMessages {
  /**
   * Message indicating a user was created successfully.
   */
  static USER_CREATED = "User created successfully";
  /**
   * Message indicating a user was updated successfully.
   */
  static USER_UPDATED = "User updated successfully";
  /**
   * Message indicating a user was deleted successfully.
   */
  static USER_DELETED = "User deleted successfully";
  /**
   * Message indicating a user was fetched successfully.
   */
  static USER_FETCHED = "User fetched successfully";
  /**
   * Message indicating a user was not found.
   */
  static USER_NOT_FOUND = "User not found";
  /**
   * Message indicating user data is missing.
   */
  static USER_DATA_MISSING = "User data is missing";
  /**
   * Message indicating a user already exists.
   */
  static USER_ALREADY_EXISTS = "User already exists";
  /**
   * Message indicating an invalid user session.
   */
  static USER_INVALID_SESSION = "Invalid session";
}

export default UserMessages;
